meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "login": "ademir@mail.com",
    "password": "10203040"
  }
}

vars:pre-request {
  baseUrl: http://localhost:8081
}

script:post-response {
  const data = res.getBody()
  bru.setEnvVar("token", data.token)
}
